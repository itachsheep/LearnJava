<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Black.NoTitleBar.Fullscreen">

        <!--<item name="android:background">@drawable/bg</item>-->
        <item name="android:windowBackground">@drawable/bg</item>
    </style>


    <style name="tip_icon_up">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:scaleType">center</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="nav_menu_item">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:ellipsize">marquee</item>
        <item name="android:gravity">center</item>
        <item name="android:button">@null</item>
        <item name="android:singleLine">true</item>
        <item name="android:textColor">@drawable/nav_item_text_selector</item>
        <item name="android:textSize">@dimen/font46</item>
        <item name="android:background">@drawable/nav_item_selector</item>
    </style>

    <style name="normal_textview">
        <!--<item name="android:textColor">@drawable/nav_item_text_selector</item>
        <item name="android:background">@drawable/ms_input_normal</item>-->
        <item name="android:textSize">@dimen/font46</item>
    </style>
</resources>
